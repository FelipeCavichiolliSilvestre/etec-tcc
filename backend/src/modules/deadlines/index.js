const { DeadlineController } = require("./DeadlineController");
const { DeadlineRepository } = require("./DeadlineRepository");
const { DeadlineService } = require("./DeadlineService");
const { DeadlineServiceValidator } = require("./DeadlineServiceValidator");

module.exports = {
  DeadlineController,
  DeadlineRepository,
  DeadlineService,
  DeadlineServiceValidator,
};
